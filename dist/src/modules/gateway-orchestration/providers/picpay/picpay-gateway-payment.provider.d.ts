import type { IGatewayPaymentProvider } from '../../contracts/gateway-payment-provider.interface';
import { GatewayPaymentDtoIn } from '../../dtos/gateway-payment.dto-in';
import { GatewayPaymentDtoOut } from '../../dtos/gateway-payment.dto-out';
export declare class PicPayGatewayPaymentProvider implements IGatewayPaymentProvider {
    getProviderName(): string;
    supports(gatewayProvider: string): boolean;
    processPayment(dtoIn: GatewayPaymentDtoIn): Promise<GatewayPaymentDtoOut>;
    private resolveAccessToken;
    private buildPicPayOrderNumber;
    private extractPaymentLinkPublicId;
    private resolvePicPayPaymentMethods;
    private resolveTokenUrl;
    private resolvePicPayExpirationDate;
    private buildPaymentLinkRequestPayload;
    private limitText;
    private parsePicPayResponse;
    private extractPaymentLinkId;
    private extractCheckoutUrl;
    private extractPicPayErrorMessage;
    private mapPicPayStatusToInternalStatus;
    private mapPicPayStatusToProcessStatus;
    private resolveBaseUrl;
    private resolveApiPath;
    private asObject;
    private toNullableString;
    private toPositiveInteger;
    private nowAsSqlDateTime;
    private formatDateToSqlDateTime;
    private pad;
    private normalize;
}
